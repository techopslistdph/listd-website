export interface ListingType {
  id: string;
  name: string;
  disabled: boolean;
  createdAt: string;
  updatedAt: string;
}

export interface PropertyType {
  id: string;
  name: string;
  disabled: boolean;
  createdAt: string;
  updatedAt: string;
}
